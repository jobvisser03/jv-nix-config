# Shared secrets - available to all hosts
#
# This file must be encrypted with sops before use!
# All hosts listed in .sops.yaml under the shared rule can decrypt this.
#
# Steps to create:
# 1. Ensure .sops.yaml has all host keys configured
# 2. Run: sops secrets/shared.yaml
# 3. Add shared secrets in YAML format
# 4. Save and close

# PLACEHOLDER - DELETE THIS FILE AND CREATE WITH SOPS
# Example shared secrets:
# shared_api_token: "token-used-by-multiple-hosts"
